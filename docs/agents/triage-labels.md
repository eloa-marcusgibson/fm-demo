# Triage labels

Canonical names equal their label strings: needs-triage, needs-info,
ready-for-agent, ready-for-human, wontfix. Plus in-progress = an agent has
claimed the issue (spawn guard). Every triage comment starts with:
> *This was generated by AI during triage.*
